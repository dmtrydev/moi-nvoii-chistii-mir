# Настройки деплоя на Netlify
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command = "npm run build"
  publish = "dist"

# SPA: все маршруты перенаправлять на index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
