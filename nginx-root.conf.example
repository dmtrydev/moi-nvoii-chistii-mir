# Статика из /root (без Docker). Если сайт в Docker Compose на порту 3001 — см. nginx-docker-proxy.conf.example
# Замените server_name, затем: sudo nginx -t && sudo systemctl reload nginx
server {
    listen 80;
    listen [::]:80;
    server_name 5.129.200.61;

    root /root;
    index index.html;

    access_log /var/log/nginx/moinoviichistiimir_access.log;
    error_log  /var/log/nginx/moinoviichistiimir_error.log;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    location /assets/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location ~* \.(ico|svg|png|jpg|jpeg|gif|webp|woff2?)$ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=86400";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
